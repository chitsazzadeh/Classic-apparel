const App = () => {
    return (
        <div>
            <h1>Welcome to the React-powered Classic Apparels</h1>
            <p>This is a sample React component inside the checkout page.</p>
        </div>
    );
};

// Render the React component into the root div
ReactDOM.render(<App />, document.getElementById('root'));
